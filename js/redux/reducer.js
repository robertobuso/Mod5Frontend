const initialState = {
         id: 0,
         user_id: 0,
	       items: [],
	       tasks: [],
         audio: ''
       }

const reducer = (state = initialState, action) => {
  switch (action.type) {
     default:
       return state
   }
}

export default reducer
